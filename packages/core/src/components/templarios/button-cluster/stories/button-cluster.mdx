import { Canvas, Controls, Meta } from '@storybook/blocks';
import * as buttonClusterStories from '../stories/button-cluster.stories';

<Meta of={buttonClusterStories} />

# tp-button-cluster

## Atributos

<Canvas of={buttonClusterStories.Default} />

<Controls />

## Métodos

### `TpButtonCluster.toggle(event?: Event)`

- O método **toggle** pode ser chamado para inverted a propriedade **collapsed** do componente.
